use board;
use pmove;
use player::player::Player;

pub struct AI {}


impl AI {
    fn new() -> AI {
        AI{}
    }
}

/*impl UI for AI {
    fn play(board: board::Board, player: player::Play) -> pmove::Move {
    }
}*/
